import React from 'react'

interface ApplyButtonProps {
    kind: string,
    description: string
}

const ApplyButton: React.FC<ApplyButtonProps> = ({kind, description}) => {
    return (
        <></>
    )
};

export default ApplyButton;